This is the synchronize_signals_module-write-a-block package meant as a guide to building
out-of-tree packages. To use the synchronize_signals_module blocks, the Python namespaces
is in 'synchronize_signals_module', which is imported as:

    import synchronize_signals_module

See the Doxygen documentation for details about the blocks available
in this package. A quick listing of the details can be found in Python
after importing by using:

    help(synchronize_signals_module)
